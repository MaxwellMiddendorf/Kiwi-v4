on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: MaxwellMidendorf/Kiwi-v4/config/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: "config/build.yaml"
    
